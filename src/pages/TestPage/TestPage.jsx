import { CVForm } from "@/features";
import { Container } from "@/shared";

export const TestPage = () => {
  return (
    <div>
      <Container>
        <CVForm />
      </Container>
    </div>
  );
};
